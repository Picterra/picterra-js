before_script:
  - npm install
  - npm ci --cache .npm --prefer-offline
build:
  image: node:latest
  stage: build
  script:
    - npm run lint
    - npm run build
  artifacts:
    expire_in: 1 week
    paths:
      - dist
unit test:
  image: node:latest
  stage: test
  script:
    - npm test